/**
 * For receive parameters on command line.
 * argv[0] -> Stores node executable.
 * argv[1] -> Stores the program.js we're running.
 * argv[2...N] -> Store others parameters passed on commandline. 
 */
 console.log(process.argv);